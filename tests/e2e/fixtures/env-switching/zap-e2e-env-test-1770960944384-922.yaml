project: e2e-env-test-1770960944384-922
native:
  echo-service:
    cmd: >
      node -e "console.log('Service started with TEST_VALUE=' + process.env.TEST_VALUE);
      setInterval(() => console.log('Heartbeat: TEST_VALUE=' + process.env.TEST_VALUE + ', NODE_ENV=' + process.env.NODE_ENV), 2000)"

env_files:
  default:
    - .env.default
  alternate:
    - .env.alternate