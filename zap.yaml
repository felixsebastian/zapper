project: lexiquest
processes:
  - name: test
    cmd: echo $TESTENV
  - name: server
    cmd: node -e "console.log('Server started on port 3000'); setInterval(() => console.log('Server running...'), 5000);" 