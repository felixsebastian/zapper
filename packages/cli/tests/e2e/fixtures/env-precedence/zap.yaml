project: env-precedence

native:
  app:
    cmd: >
      node -e "console.log('test process')"
    env: "*"

env:
  default:
    - .env.shared
    - .env.local
    - .env.user
  remote:
    - .env.shared
    - .env.remote
    - .env.user.remote
